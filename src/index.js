/** M-PESA utility class */
class Mpesa {
    #env;

    /**
     * M-PESA initialization
     * @param {string} env
     */
    constructor(env) {
        this.#env = env;
    }
}

export default Mpesa;

